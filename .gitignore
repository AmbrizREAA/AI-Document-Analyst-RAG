# Environment variables and secrets
.env
.env.local
*.pkl

# Virtual environments
venv/
env/
.venv/

# Python build / cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/

# Persistent ChromaDB vector store (generated at runtime — never commit user data or embeddings)
chroma_db/
storage/chroma/

# Legacy FAISS vector stores (no longer used; ignored so old folders aren't committed)
vector_stores/

# Uploaded / processed user documents (never commit user data)
uploads/
processed_documents/

# Local databases
*.db
*.sqlite3

# Editors / IDEs
.idea/
.vscode/

# OS junk
.DS_Store
Thumbs.db
