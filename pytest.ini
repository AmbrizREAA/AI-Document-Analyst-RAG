[pytest]
pythonpath = .
testpaths = tests
addopts = -ra -q
markers =
    integration: requires a live PostgreSQL connection (reads DATABASE_URL from .env)
    chroma: requires a working local ChromaDB + embedding model (slower)
